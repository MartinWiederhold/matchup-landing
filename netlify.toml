[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — react-router-dom client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
